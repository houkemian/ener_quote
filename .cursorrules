# EnerQuote (光储大师) - Fullstack AI Assistant Guide

## 1. Project Overview
EnerQuote 是一款面向全球 2026 年市场的 B2B SaaS 系统。主要为光伏与储能 (PV+ESS) 集成商提供 8760 小时能量流闭环测算、拉美高息金融测算、Paddle Billing 支付墙以及企业级 PDF 建议书导出功能。

## 2. Monorepo Architecture
这是一个前后端分离的 Monorepo 项目，分为 `/frontend` 和 `/backend` 两个核心目录。

### [Backend] Stack: Python + FastAPI + SQLite + SQLAlchemy
* **Path:** `/backend`
* **Core Engine:** * `app/engine/physics.py`: 8760小时物理能量流计算引擎（光伏发电、电池充放电、需量削峰、谷电套利）。
  * `app/engine/finance.py`: 财务引擎（NPV、IRR、LCOE、投资回收期分析）。
* **Database:** SQLite (`pv_ess.db`)。ORM 使用 SQLAlchemy。包含多租户架构 (`companies`)，IAM 身份系统 (`users`) 和动态硬件配置库。
* **Third-Party APIs:**
  * Paddle Billing API: 处理 PRO 会员结账及 Webhook 自动提权 (`app/api/v1/payment.py`)。
  * PVGIS API: 跨国拉取真实经纬度的 8760 光照数据 (`app/services/pvgis.py`)。
* **Auth:** 基于 JWT 的鉴权，Token 中携带有 `tier` (如 FREE/PRO) 以进行权限隔离。
* **Run Command:** `cd backend && uvicorn app.main:app --reload`

### [Frontend] Stack: Flutter + Provider + Dio + fl_chart
* **Path:** `/frontend`
* **Network:** 使用 Dio 封装拦截器 (`lib/core/network/api_client.dart`)，支持 401 自动登出与 JWT Bearer 注入。
* **UI/UX:** 横屏优先 (Landscape) 仪表盘，亮色 SaaS 主题，响应式 Slider 交互。
* **I18n (Internationalization):** 严格遵守 `flutter_localizations`。语言包位于 `/lib/l10n/` (`app_zh.arb`, `app_en.arb`)。**绝对禁止在 UI 代码中硬编码中文字符，必须使用 `AppLocalizations.of(context)`。**
* **Core Features:**
  * Sentry: 崩溃监控与性能追踪。
  * Printing/PDF: 纯 Dart 绘制多页商业级 PDF 报告 (`lib/utils/pdf_export.dart`)。
  * Paddle Web Checkout: 使用 `url_launcher` 唤起外部浏览器打开托管结账页。

## 3. Cursor AI Coding Rules (Must Follow)
1. **Context Awareness:** 修改前后端交互逻辑时，必须同时考虑 `/backend/app/api/` 的接口定义与 `/frontend/lib/core/network/` 的调用。
2. **Localization First:** 为 Frontend 生成新 UI 时，如果涉及文本，必须提示我更新 `.arb` 字典文件，并使用 `l10n.xxx` 调用。
3. **Safe Database Migrations:** 修改 Backend SQLAlchemy Models 时，必须提供原生的 SQLite `ALTER TABLE` SQL 脚本或 Alembic 指导。
4. **Code Generation:** 输出代码时，请明确指出该代码所属的文件路径（例如 `// Path: frontend/lib/screens/xxx.dart`）。
5. **No Placeholders:** 尽量给出完整的代码替换逻辑，避免使用 `// ... existing code` 导致代码遗漏。
6. **B2B Mindset:** 保持 B2B SaaS 的严谨性，特别是关于财务公式（IRR、现金流）和能量守恒的逻辑，必须绝对精确。
org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Google Sign-In — Web 应用客户端 ID（*.apps.googleusercontent.com），用作 ID Token 的 audience。
# 须与 --dart-define=GOOGLE_SERVER_CLIENT_ID 及后端 GOOGLE_OAUTH_CLIENT_IDS 中的 Web 客户端一致。
# 另须在 Google Cloud 创建「Android」OAuth 客户端：包名 one.dothings.enerquote，SHA-1 与当前签名一致
# （本机调试：keytool -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android）。
# 错误 12500 = 包名/SHA-1/Web 客户端与控制台不一致。
GOOGLE_SERVER_CLIENT_ID=191848630682-d0imac6vatistl9o2vnhv8qrtkrp2dj9.apps.googleusercontent.com
